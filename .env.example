BOT_TOKEN=PASTE_YOUR_TOKEN_HERE
# Admin(s): ADMIN_IDS (comma-separated) or ADMIN_ID (single)
ADMIN_IDS=7700265732
# Automatic reminders: 1=enabled, 0=disabled (default: 1)
REMINDERS_ENABLED=1
# Penalty per overdue day (so'm), used if rental has no penalty_per_day. 0=no penalty. Default: 2000
PENALTY_PER_DAY_DEFAULT=2000
